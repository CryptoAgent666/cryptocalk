---
import LocalizedCalculatorPage from '../../components/LocalizedCalculatorPage.astro';
import TradeExpectancyCalculator from '../../components/TradeExpectancyCalculator.tsx';
import type { Lang } from '../../i18n/utils';

export function getStaticPaths() {
  return ['es', 'pt', 'tr', 'hi', 'ru'].map((lang) => ({ params: { lang } }));
}

const { lang } = Astro.params as { lang: Lang };
---

<LocalizedCalculatorPage
  lang={lang}
  slug="trade-expectancy-calculator"
  title="Trade Expectancy Calculator"
  description="Estimate strategy edge per trade and project monthly outcomes using win rate, R-multiples, and risk per trade."
>
  <TradeExpectancyCalculator slot="calculator" lang={typeof lang !== 'undefined' ? lang : 'en'} client:load />
</LocalizedCalculatorPage>
