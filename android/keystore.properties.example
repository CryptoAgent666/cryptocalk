# Android signing credentials — copy to keystore.properties and fill with real values.
# This file is gitignored (see android/.gitignore).
# Used by android/app/build.gradle to sign release builds.

storeFile=app/cryptocalk-release-key.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=cryptocalk
keyPassword=YOUR_KEY_PASSWORD
