# CoinGecko Demo API Key (PUBLIC_ prefix = available in client-side code)
PUBLIC_COINGECKO_API_KEY=your-coingecko-api-key

# CryptoCompare API Key — SERVER-SIDE ONLY (no PUBLIC_ prefix: must never ship in the
# client bundle; free tier is 100 calls/month and visitors exhausted it in 2026-07).
# Used only by scripts/bake-price-history.mjs for rare pre-listing backfills.
CRYPTOCOMPARE_API_KEY=your-cryptocompare-api-key
