# Copy to scripts/ota.env and fill in. Sourced by scripts/ota-publish.sh (VPS mode).
# scripts/ota.env is your private deploy config — do not commit it.

OTA_SSH="root@176.97.68.234"
OTA_REMOTE_DIR="/var/www/www-root/data/www/ota.cryptocalk.com"
OTA_SSH_PORT="22"
